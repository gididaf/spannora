// @spannora/shared — re-exports of the frontend modules consumed by both
// the in-server PWA (packages/server/public/) and the hub PWA
// (packages/hub/). Bodies land in subsequent commits.
export {};
